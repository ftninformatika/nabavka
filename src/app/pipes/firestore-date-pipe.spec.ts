import { FirestoreDatePipe } from './firestore-date-pipe';

describe('FirestoreDatePipe', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new FirestoreDatePipe()).toBeTruthy();
  });
});
