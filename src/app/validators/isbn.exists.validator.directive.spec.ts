import { IsbnExistsValidatorDirective } from './isbn.exists.validator.directive';

describe('Isbn.Exists.ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IsbnExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
